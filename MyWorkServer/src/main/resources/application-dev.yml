spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mywork
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:3000/eureka # 必须eureka结尾
